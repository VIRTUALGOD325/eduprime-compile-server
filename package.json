{
  "name": "eduprime-compile-server",
  "version": "1.0.0",
  "description": "Cloud Arduino compiler for EduPrime Hardware — returns compiled .hex for browser flashing",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
